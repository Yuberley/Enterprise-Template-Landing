---
---

<section class="hero">
	<div class="container">
		<div class="hero-content">

			<h1 class="hero-title animate-fade-in-up">
				De <span class="gradient-text">Meses a Minutos</span><br/>
				con Inteligencia Artificial
			</h1>

			<p class="hero-description animate-fade-in-up">
				Template empresarial completo que crea la base de código lista para iterar proyectos 
				<strong>Backend (.NET)</strong>, <strong>Frontend (React)</strong> y <strong>Mobile (React Native)</strong> 
				con arquitectura limpia, autenticación, roles, storage, base de datos, caché y logs. 
				<span class="highlight">Todo dockerizado y listo para IA en ~10 minutos.</span>
			</p>

			<div class="hero-stats animate-fade-in-up">
				<div class="stat">
					<div class="stat-value">10min</div>
					<div class="stat-label">Setup completo</div>
				</div>
				<div class="stat-divider"></div>
				<div class="stat">
					<div class="stat-value">3 Plataformas</div>
					<div class="stat-label">Backend, Web, Mobile</div>
				</div>
				<div class="stat-divider"></div>
				<div class="stat">
					<div class="stat-value">100% IA Ready</div>
					<div class="stat-label">Itera con Copilot</div>
				</div>
			</div>

			<div class="hero-cta animate-fade-in-up">
				<a href="#contact" class="btn btn-primary">
					<svg width="20" height="20" viewBox="0 0 20 20" fill="none">
						<path d="M3 8L10 13L17 8M3 12L10 17L17 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
					</svg>
					Agendar una Llamada
				</a>
				<a href="#demo" class="btn btn-secondary">
					<svg width="20" height="20" viewBox="0 0 20 20" fill="none">
						<path d="M6 4L14 10L6 16V4Z" fill="currentColor"/>
					</svg>
					Ver Demo
				</a>
			</div>

			<div class="hero-proof animate-fade-in">
				<div class="proof-item">
					<svg width="20" height="20" viewBox="0 0 20 20" fill="none">
						<path d="M10 2L12.09 7.26L18 8.27L14 12.14L15.18 18.02L10 15.27L4.82 18.02L6 12.14L2 8.27L7.91 7.26L10 2Z" fill="#10b981"/>
					</svg>
					<span>Arquitectura Clean & CQRS</span>
				</div>
				<div class="proof-item">
					<svg width="20" height="20" viewBox="0 0 20 20" fill="none">
						<path d="M10 2L12.09 7.26L18 8.27L14 12.14L15.18 18.02L10 15.27L4.82 18.02L6 12.14L2 8.27L7.91 7.26L10 2Z" fill="#10b981"/>
					</svg>
					<span>Integración Azure DevOps</span>
				</div>
				<div class="proof-item">
					<svg width="20" height="20" viewBox="0 0 20 20" fill="none">
						<path d="M10 2L12.09 7.26L18 8.27L14 12.14L15.18 18.02L10 15.27L4.82 18.02L6 12.14L2 8.27L7.91 7.26L10 2Z" fill="#10b981"/>
					</svg>
					<span>Production Ready</span>
				</div>
			</div>
		</div>

		<div class="hero-visual animate-fade-in">
			<div class="code-window">
				<div class="code-header">
					<div class="code-dots">
						<span></span>
						<span></span>
						<span></span>
					</div>
					<div class="code-title">GitHub Copilot Chat</div>
				</div>
				<div class="code-content">
					<div class="code-line">
						<span class="prompt">></span>
						<span class="text">Crear un proyecto nuevo llamado <span class="highlight">Cristal.Ecommerce</span> con el</span>
					</div>
					<div class="code-line">
						<span class="text">Enterprise Template, sigue los pasos de su instalación, quiero que</span>
					</div>
					<div class="code-line">
						<span class="text">uses <span class="tech">Minio</span>, <span class="tech">Postgres</span> y <span class="tech">Redis</span>.</span>
					</div>
					<div class="code-line response">
						<span class="ai-icon">🤖</span>
						<span class="text">Perfecto! Iniciando configuración del Enterprise Template...</span>
					</div>
					<div class="code-line response">
						<span class="check">✓</span>
						<span class="text success">Backend (.NET) con Clean Architecture</span>
					</div>
					<div class="code-line response">
						<span class="check">✓</span>
						<span class="text success">Frontend (React) + Dashboard</span>
					</div>
					<div class="code-line response">
						<span class="check">✓</span>
						<span class="text success">Mobile (React Native + Expo)</span>
					</div>
					<div class="code-line response">
						<span class="check">✓</span>
						<span class="text success">Docker Compose configurado</span>
					</div>
					<div class="code-line response loading">
						<span class="loader"></span>
						<span class="text">Configurando Keycloak, Minio, Postgres, Redis...</span>
					</div>
				</div>
			</div>
		</div>
	</div>

	<div class="hero-gradient"></div>
</section>

<style>
	.hero {
		position: relative;
		min-height: 100vh;
		display: flex;
		align-items: center;
		padding: var(--spacing-3xl) 0;
		overflow: hidden;
	}

	.container {
		max-width: 1280px;
		margin: 0 auto;
		padding: 0 var(--spacing-lg);
		width: 100%;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: var(--spacing-3xl);
		align-items: center;
	}

	.hero-content {
		z-index: 1;
	}

	.badge {
		display: inline-flex;
		align-items: center;
		gap: var(--spacing-xs);
		padding: var(--spacing-xs) var(--spacing-md);
		background: linear-gradient(135deg, rgba(99, 102, 241, 0.1), rgba(245, 158, 11, 0.1));
		border: 1px solid rgba(99, 102, 241, 0.2);
		border-radius: 50px;
		font-size: 0.875rem;
		color: var(--color-text-secondary);
		margin-bottom: var(--spacing-lg);
	}

	.badge svg {
		color: var(--color-accent);
	}

	.hero-title {
		font-size: clamp(2.5rem, 6vw, 4rem);
		font-weight: 900;
		margin-bottom: var(--spacing-lg);
		line-height: 1.1;
		animation-delay: 0.1s;
	}

	.gradient-text {
		background: linear-gradient(135deg, var(--color-primary-light), var(--color-accent));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.hero-description {
		font-size: 1.125rem;
		color: var(--color-text-secondary);
		margin-bottom: var(--spacing-xl);
		line-height: 1.8;
		max-width: 90%;
		animation-delay: 0.2s;
	}

	.hero-description strong {
		color: var(--color-text);
		font-weight: 600;
	}

	.hero-description .highlight {
		color: var(--color-primary-light);
		font-weight: 600;
	}

	.hero-stats {
		display: flex;
		align-items: center;
		gap: var(--spacing-lg);
		margin-bottom: var(--spacing-xl);
		padding: var(--spacing-lg);
		background: var(--color-bg-secondary);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-lg);
		animation-delay: 0.3s;
	}

	.stat {
		text-align: center;
	}

	.stat-value {
		font-size: 1.5rem;
		font-weight: 700;
		color: var(--color-primary-light);
		margin-bottom: var(--spacing-xs);
	}

	.stat-label {
		font-size: 0.875rem;
		color: var(--color-text-muted);
	}

	.stat-divider {
		width: 1px;
		height: 40px;
		background: var(--color-border);
	}

	.hero-cta {
		display: flex;
		gap: var(--spacing-md);
		margin-bottom: var(--spacing-xl);
		animation-delay: 0.4s;
	}

	.btn {
		display: inline-flex;
		align-items: center;
		gap: var(--spacing-xs);
		padding: var(--spacing-md) var(--spacing-lg);
		border-radius: var(--radius-md);
		font-weight: 600;
		font-size: 1rem;
		transition: all 0.3s ease;
		cursor: pointer;
		border: none;
		text-decoration: none;
	}

	.btn-primary {
		background: linear-gradient(135deg, var(--color-primary), var(--color-primary-dark));
		color: white;
		box-shadow: var(--shadow-lg);
	}

	.btn-primary:hover {
		transform: translateY(-2px);
		box-shadow: var(--shadow-xl), var(--shadow-glow);
	}

	.btn-secondary {
		background: var(--color-bg-secondary);
		color: var(--color-text);
		border: 1px solid var(--color-border);
	}

	.btn-secondary:hover {
		background: var(--color-bg-tertiary);
		border-color: var(--color-primary);
	}

	.hero-proof {
		display: flex;
		flex-wrap: wrap;
		gap: var(--spacing-md);
		animation-delay: 0.5s;
	}

	.proof-item {
		display: flex;
		align-items: center;
		gap: var(--spacing-xs);
		font-size: 0.875rem;
		color: var(--color-text-muted);
	}

	/* Code Window */
	.hero-visual {
		position: relative;
		animation-delay: 0.3s;
	}

	.code-window {
		background: var(--color-bg-secondary);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-xl);
		overflow: hidden;
		box-shadow: var(--shadow-xl);
	}

	.code-header {
		display: flex;
		align-items: center;
		gap: var(--spacing-md);
		padding: var(--spacing-md) var(--spacing-lg);
		background: var(--color-bg-tertiary);
		border-bottom: 1px solid var(--color-border);
	}

	.code-dots {
		display: flex;
		gap: var(--spacing-xs);
	}

	.code-dots span {
		width: 12px;
		height: 12px;
		border-radius: 50%;
		background: var(--color-border);
	}

	.code-dots span:nth-child(1) {
		background: #ef4444;
	}

	.code-dots span:nth-child(2) {
		background: #f59e0b;
	}

	.code-dots span:nth-child(3) {
		background: #10b981;
	}

	.code-title {
		font-size: 0.875rem;
		color: var(--color-text-muted);
	}

	.code-content {
		padding: var(--spacing-lg);
		font-family: 'Fira Code', 'Monaco', monospace;
		font-size: 0.875rem;
		line-height: 1.8;
	}

	.code-line {
		display: flex;
		gap: var(--spacing-sm);
		margin-bottom: var(--spacing-sm);
	}

	.code-line .prompt {
		color: var(--color-primary-light);
		font-weight: 700;
	}

	.code-line .text {
		color: var(--color-text-secondary);
	}

	.code-line .highlight {
		color: var(--color-accent);
		font-weight: 600;
	}

	.code-line .tech {
		color: var(--color-primary-light);
		font-weight: 600;
	}

	.code-line.response {
		padding-left: var(--spacing-md);
		border-left: 2px solid var(--color-primary);
	}

	.code-line .ai-icon {
		font-size: 1rem;
	}

	.code-line .check {
		color: var(--color-success);
		font-weight: 700;
	}

	.code-line .text.success {
		color: var(--color-success);
	}

	.code-line.loading {
		animation: glow 2s ease-in-out infinite;
		align-items: center;
	}

	.loader {
		display: inline-block;
		width: 16px;
		height: 16px;
		border: 2px solid var(--color-border);
		border-top-color: var(--color-primary);
		border-radius: 50%;
		animation: spin 1s linear infinite;
		flex-shrink: 0;
	}

	@keyframes spin {
		to {
			transform: rotate(360deg);
		}
	}

	.hero-gradient {
		position: absolute;
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
		width: 600px;
		height: 600px;
		background: radial-gradient(circle, rgba(99, 102, 241, 0.15), transparent 70%);
		filter: blur(60px);
		pointer-events: none;
		z-index: 0;
	}

	@media (max-width: 968px) {
		.container {
			grid-template-columns: 1fr;
			gap: var(--spacing-xl);
		}

		.hero {
			padding: var(--spacing-xl) 0;
		}

		.hero-title {
			font-size: 2.5rem;
		}

		.hero-description {
			max-width: 100%;
		}

		.hero-stats {
			flex-direction: column;
			gap: var(--spacing-md);
		}

		.stat-divider {
			display: none;
		}

		.hero-cta {
			flex-direction: column;
		}

		.btn {
			width: 100%;
			justify-content: center;
		}
	}
</style>
